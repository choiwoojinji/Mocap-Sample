# TODO / 진행 기록

프로젝트 진행 상황의 단일 기준점. 작업이 끝나면 "다음 할 일"에서 "완료 로그"로 옮기고
날짜를 적는다. 조건별 악조건 대응 상태는 `design.md`의 ODD 표가 별도 관리.

## 다음 할 일 (우선순위 순)

- [ ] **데이터 보강** — 라벨당 100개 목표 (현재 6클래스 × 30개)
  - [ ] 기존 6클래스 각 +70개 (수집 태스크 재실행, 이어서 쌓임)
  - [ ] `back` 라벨 신규 수집 (손등으로 밀어내기)
  - [ ] `idle` 확대: 손 안 보이는 변형(팔 내림, 주머니, 뒤돌기) 포함 150~200개
  - [ ] 재학습 → 목표 test 90%+ (현재 77.8%)
- [ ] **악조건 실측 테스트** (design.md ODD A표의 ⬜ 해소)
  - [ ] 저조도(불 끄고) / 역광 / 목장갑 착용 상태로 신호별 인식 확인
  - [ ] 오인식(엉뚱한 신호로 확정) 사례 발견 시 해당 조건 실측 데이터 수집
- [ ] **오분류 조준 보강** — 실사용에서 빗나가는 신호 관찰 → 그 라벨/상황만 추가 수집

## 백로그 (제품화 단계 — 급하지 않음)

- [ ] 불확실성 로깅(액티브 러닝): 추론 중 저신뢰 시퀀스 자동 저장 → 조준 수집 목록화
- [ ] 시야 자가 진단 채널 `/vision_health` (design.md C절 사양 참고)
  — 렌즈 부분 가림(벌레·기름때) 등 지속·부분 열화 감시, DEGRADED → 감속+알람
- [x] ~~안정화 히스테리시스~~ → "다음 할 일"의 '확정 해제 비대칭 수정'으로 승격
- [ ] Mocap-Sim 점진 감속 고도화: 사람 확률 게이트(YOLO류 탐지) 결합 — 거리 구역 감속 자체는
  라이다 기반으로 구현 완료(2026-07-09), 남은 것은 "사람이냐 물체냐" 구분 (〃 2절)
- [ ] Mocap-Sim 경로 계획: come 접근 시 장애물 회피 — 인공 포텐셜 필드 (〃 3절)
- [ ] 이미지 전처리(노출·CLAHE) — 저조도/역광 실측에서 문제 확인되면 도입
- [ ] 다중 사용자 일반화: 여러 사람 수집, 사람 단위 분할 평가, (필요시) NATOPS 사전학습
- [ ] Mocap-Sim: 월드 확장(장애물·작업 구역), 네이티브 Ubuntu+GPU 이전 검토

## 완료 로그

### 2026-07-10
- 우분투 더블클릭 실행: `tools/install_launcher.sh`(1회) → 바탕화면/앱 목록의
  "Mocap-Sim 시뮬레이터" 아이콘으로 실행 (우클릭에 헤드리스 옵션)
- **VM Gazebo 크래시 재발 해결**: 시스템 업데이트(Mesa 25.2.8)로 상황 역전 —
  과거 필수였던 `LIBGL_ALWAYS_SOFTWARE=1`이 EGL 세그폴트의 원인이 됨.
  기본값을 "강제 없음(Mesa 자동)"으로 뒤집고 구형 Mesa용 `FORCE_SW=1` 탈출구 유지.
  `.bashrc` 잔재도 스크립트가 unset. README 현행화

### 2026-07-09
- **Mocap-Sim 구역 기반 점진 감속** (physical-ai-notes 2절 적용): signal_listener에
  `속도 = min(신호, 구역 상한)` — 위험(<1m) 전진 금지 / 경고(<2m) 0.1 m/s / 안전 무제한.
  월드에 검증용 장애물 2개. 신호(의도)와 구역(물리 안전)을 독립 방어선으로 분리.
  전방 거리 소스 이원화: 라이다 실측(`lidar:=true`, factory_lidar.sdf, **네이티브 GPU 전용**
  — VM 소프트웨어 렌더링에서 센서 렌더링 세그폴트 실측 확인) / 오도메트리+장애물 좌표
  ground truth(기본 월드, VM 경로). 라이다 수신 중이면 실측이 우선
- Mocap-Sim 우분투 원클릭 실행 스크립트 `tools/run_sim.sh` (환경→빌드→launch 일괄,
  VM 자동 감지 시 CPU 렌더링) — README 실행 절차 현행화
- 프로젝트 구조 PDF 작성(`doc/project-structure.pdf`) — ML 방법론·라이브러리 선택
  근거(Keras/YOLO/픽셀 학습 대비) 포함
- **확정 해제 비대칭 수정** (구조 점검에서 발견): 확률 EMA 평활(`--ema`) + 해제 유예
  히스테리시스(`--release-grace` 1초) 도입 — 스파이크 노이즈 1프레임에 기계가 서던
  구조 제거. 사람 미감지 가드·stop 전환은 유예 없이 즉시(안전 비대칭 유지). 실카메라 검증
- 피지컬 AI 기법 노트 작성(`physical-ai-notes.md`) + 이를 잣대로 구조 점검 수행
- 악조건 증강(특징 수준 도메인 랜덤화) 구현 — `src/training/augment.py`
  (노이즈/손 소실/시간 신축/이동·스케일/렌즈 왜곡, 부재 지점 보존, val/test 미적용)
- 스트레스 테스트 도구 — `src/training/stress_test.py`. 전 조건 하락 2%p 이내 확인
- 악조건 운영 설계(ODD)·고장 유형 대응표 문서화 — design.md

### 2026-07-07
- **end-to-end 실증**: 웹캠 → rosbridge → ROS 2 `/hand_signal` → Gazebo 기계 반응
  (UTM VM: Ubuntu 24.04 + ROS 2 Jazzy + Gazebo Harmonic, CPU 렌더링)
- 자체 수집 데이터 학습: 6클래스(stop/slow/come/left_go/right_go/idle) × 30개, test 77.8%
- 결과 전달 인터페이스 — `publisher.py` (rosbridge/UDP, 1초 하트비트, unknown 처리)
- Mac 단독 데모 구축 (Gazebo brew 네이티브 + UDP 어댑터 + 통합 런처 태스크)
- 추론 안전 가드(사람 미감지 → unknown), 확률 패널 UI, 손 이탈 경고(3개 창 공통)
- Mocap-Sim 저장소 분리(git init), 문서 전체 현행화, README 작성

### 2026-07-06
- 특징 확장: 손 126 → **손+상체 포즈 150차원** (`extractor.py`) — 장갑·팔 신호 대비
- Jester 실험: 다운로드(11.9GB)→변환→학습 test 88~91%였으나 **실사용 실패
  (도메인 갭 + 좌우 반전) → 자체 수집 단독으로 전환** (핵심 교훈, ml-flow.md 1-2절)
- 학습 프레임워크 구축 — PyTorch LSTM, 클래스 동적 구성, early stopping, 혼동 행렬
- 실시간 추론 구축 — 슬라이딩 윈도우 + 안정화 필터(신뢰도 0.8 + 연속 5회)
- 클래스 개편: 멈춰+오지마 통합, 좌/우 추가 → 이후 영어 라벨로 전환

### 2026-07-05
- 수집 UX 개선: 자동 연속 수집(START 한 번), 화면 버튼, REC 진행 바(카운터 혼동 수정)
- 프로젝트 정의 확정: 수어 → **산업 수신호 인식 + 공정 기계 시뮬레이터**

### 2026-07-04
- 프로젝트 셋업: venv(3.12)·버전 고정, 크로스 플랫폼 VSCode 태스크(셋업/실행 일체)
- 랜드마크 시각화(`landmark_viewer.py`, 30 FPS 실측), 시퀀스 수집기(`collect.py`) 구축
